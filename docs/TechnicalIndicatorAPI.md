# \TechnicalIndicatorAPI

All URIs are relative to *https://api.twelvedata.com*

Method | HTTP request | Description
------------- | ------------- | -------------
[**GetTimeSeriesAd**](TechnicalIndicatorAPI.md#GetTimeSeriesAd) | **Get** /ad | Accumulation/distribution
[**GetTimeSeriesAdOsc**](TechnicalIndicatorAPI.md#GetTimeSeriesAdOsc) | **Get** /adosc | Accumulation/distribution oscillator
[**GetTimeSeriesAdd**](TechnicalIndicatorAPI.md#GetTimeSeriesAdd) | **Get** /add | Addition
[**GetTimeSeriesAdx**](TechnicalIndicatorAPI.md#GetTimeSeriesAdx) | **Get** /adx | Average directional index
[**GetTimeSeriesAdxr**](TechnicalIndicatorAPI.md#GetTimeSeriesAdxr) | **Get** /adxr | Average directional movement index rating
[**GetTimeSeriesApo**](TechnicalIndicatorAPI.md#GetTimeSeriesApo) | **Get** /apo | Absolute price oscillator
[**GetTimeSeriesAroon**](TechnicalIndicatorAPI.md#GetTimeSeriesAroon) | **Get** /aroon | Aroon indicator
[**GetTimeSeriesAroonOsc**](TechnicalIndicatorAPI.md#GetTimeSeriesAroonOsc) | **Get** /aroonosc | Aroon oscillator
[**GetTimeSeriesAtr**](TechnicalIndicatorAPI.md#GetTimeSeriesAtr) | **Get** /atr | Average true range
[**GetTimeSeriesAvg**](TechnicalIndicatorAPI.md#GetTimeSeriesAvg) | **Get** /avg | Average
[**GetTimeSeriesAvgPrice**](TechnicalIndicatorAPI.md#GetTimeSeriesAvgPrice) | **Get** /avgprice | Average price
[**GetTimeSeriesBBands**](TechnicalIndicatorAPI.md#GetTimeSeriesBBands) | **Get** /bbands | Bollinger bands
[**GetTimeSeriesBeta**](TechnicalIndicatorAPI.md#GetTimeSeriesBeta) | **Get** /beta | Beta indicator
[**GetTimeSeriesBop**](TechnicalIndicatorAPI.md#GetTimeSeriesBop) | **Get** /bop | Balance of power
[**GetTimeSeriesCci**](TechnicalIndicatorAPI.md#GetTimeSeriesCci) | **Get** /cci | Commodity channel index
[**GetTimeSeriesCeil**](TechnicalIndicatorAPI.md#GetTimeSeriesCeil) | **Get** /ceil | Ceiling
[**GetTimeSeriesCmo**](TechnicalIndicatorAPI.md#GetTimeSeriesCmo) | **Get** /cmo | Chande momentum oscillator
[**GetTimeSeriesCoppock**](TechnicalIndicatorAPI.md#GetTimeSeriesCoppock) | **Get** /coppock | Coppock curve
[**GetTimeSeriesCorrel**](TechnicalIndicatorAPI.md#GetTimeSeriesCorrel) | **Get** /correl | Correlation
[**GetTimeSeriesCrsi**](TechnicalIndicatorAPI.md#GetTimeSeriesCrsi) | **Get** /crsi | Connors relative strength index
[**GetTimeSeriesDema**](TechnicalIndicatorAPI.md#GetTimeSeriesDema) | **Get** /dema | Double exponential moving average
[**GetTimeSeriesDiv**](TechnicalIndicatorAPI.md#GetTimeSeriesDiv) | **Get** /div | Division
[**GetTimeSeriesDpo**](TechnicalIndicatorAPI.md#GetTimeSeriesDpo) | **Get** /dpo | Detrended price oscillator
[**GetTimeSeriesDx**](TechnicalIndicatorAPI.md#GetTimeSeriesDx) | **Get** /dx | Directional movement index
[**GetTimeSeriesEma**](TechnicalIndicatorAPI.md#GetTimeSeriesEma) | **Get** /ema | Exponential moving average
[**GetTimeSeriesExp**](TechnicalIndicatorAPI.md#GetTimeSeriesExp) | **Get** /exp | Exponential
[**GetTimeSeriesFloor**](TechnicalIndicatorAPI.md#GetTimeSeriesFloor) | **Get** /floor | Floor
[**GetTimeSeriesHeikinashiCandles**](TechnicalIndicatorAPI.md#GetTimeSeriesHeikinashiCandles) | **Get** /heikinashicandles | Heikinashi candles
[**GetTimeSeriesHlc3**](TechnicalIndicatorAPI.md#GetTimeSeriesHlc3) | **Get** /hlc3 | High, low, close average
[**GetTimeSeriesHtDcPeriod**](TechnicalIndicatorAPI.md#GetTimeSeriesHtDcPeriod) | **Get** /ht_dcperiod | Hilbert transform dominant cycle period
[**GetTimeSeriesHtDcPhase**](TechnicalIndicatorAPI.md#GetTimeSeriesHtDcPhase) | **Get** /ht_dcphase | Hilbert transform dominant cycle phase
[**GetTimeSeriesHtPhasor**](TechnicalIndicatorAPI.md#GetTimeSeriesHtPhasor) | **Get** /ht_phasor | Hilbert transform phasor components
[**GetTimeSeriesHtSine**](TechnicalIndicatorAPI.md#GetTimeSeriesHtSine) | **Get** /ht_sine | Hilbert transform sine wave
[**GetTimeSeriesHtTrendMode**](TechnicalIndicatorAPI.md#GetTimeSeriesHtTrendMode) | **Get** /ht_trendmode | Hilbert transform trend vs cycle mode
[**GetTimeSeriesHtTrendline**](TechnicalIndicatorAPI.md#GetTimeSeriesHtTrendline) | **Get** /ht_trendline | Hilbert transform instantaneous trendline
[**GetTimeSeriesIchimoku**](TechnicalIndicatorAPI.md#GetTimeSeriesIchimoku) | **Get** /ichimoku | Ichimoku cloud
[**GetTimeSeriesKama**](TechnicalIndicatorAPI.md#GetTimeSeriesKama) | **Get** /kama | Kaufman adaptive moving average
[**GetTimeSeriesKeltner**](TechnicalIndicatorAPI.md#GetTimeSeriesKeltner) | **Get** /keltner | Keltner channel
[**GetTimeSeriesKst**](TechnicalIndicatorAPI.md#GetTimeSeriesKst) | **Get** /kst | Know sure thing
[**GetTimeSeriesLinearReg**](TechnicalIndicatorAPI.md#GetTimeSeriesLinearReg) | **Get** /linearreg | Linear regression
[**GetTimeSeriesLinearRegAngle**](TechnicalIndicatorAPI.md#GetTimeSeriesLinearRegAngle) | **Get** /linearregangle | Linear regression angle
[**GetTimeSeriesLinearRegIntercept**](TechnicalIndicatorAPI.md#GetTimeSeriesLinearRegIntercept) | **Get** /linearregintercept | Linear regression intercept
[**GetTimeSeriesLinearRegSlope**](TechnicalIndicatorAPI.md#GetTimeSeriesLinearRegSlope) | **Get** /linearregslope | Linear regression slope
[**GetTimeSeriesLn**](TechnicalIndicatorAPI.md#GetTimeSeriesLn) | **Get** /ln | Natural logarithm
[**GetTimeSeriesLog10**](TechnicalIndicatorAPI.md#GetTimeSeriesLog10) | **Get** /log10 | Base-10 logarithm
[**GetTimeSeriesMa**](TechnicalIndicatorAPI.md#GetTimeSeriesMa) | **Get** /ma | Moving average
[**GetTimeSeriesMacd**](TechnicalIndicatorAPI.md#GetTimeSeriesMacd) | **Get** /macd | Moving average convergence divergence
[**GetTimeSeriesMacdExt**](TechnicalIndicatorAPI.md#GetTimeSeriesMacdExt) | **Get** /macdext | Moving average convergence divergence extension
[**GetTimeSeriesMacdSlope**](TechnicalIndicatorAPI.md#GetTimeSeriesMacdSlope) | **Get** /macd_slope | Moving average convergence divergence slope
[**GetTimeSeriesMama**](TechnicalIndicatorAPI.md#GetTimeSeriesMama) | **Get** /mama | MESA adaptive moving average
[**GetTimeSeriesMax**](TechnicalIndicatorAPI.md#GetTimeSeriesMax) | **Get** /max | Maximum
[**GetTimeSeriesMaxIndex**](TechnicalIndicatorAPI.md#GetTimeSeriesMaxIndex) | **Get** /maxindex | Maximum Index
[**GetTimeSeriesMcGinleyDynamic**](TechnicalIndicatorAPI.md#GetTimeSeriesMcGinleyDynamic) | **Get** /mcginley_dynamic | McGinley dynamic indicator
[**GetTimeSeriesMedPrice**](TechnicalIndicatorAPI.md#GetTimeSeriesMedPrice) | **Get** /medprice | Median price
[**GetTimeSeriesMfi**](TechnicalIndicatorAPI.md#GetTimeSeriesMfi) | **Get** /mfi | Money flow index
[**GetTimeSeriesMidPoint**](TechnicalIndicatorAPI.md#GetTimeSeriesMidPoint) | **Get** /midpoint | Midpoint
[**GetTimeSeriesMidPrice**](TechnicalIndicatorAPI.md#GetTimeSeriesMidPrice) | **Get** /midprice | Midprice
[**GetTimeSeriesMin**](TechnicalIndicatorAPI.md#GetTimeSeriesMin) | **Get** /min | Minimum
[**GetTimeSeriesMinIndex**](TechnicalIndicatorAPI.md#GetTimeSeriesMinIndex) | **Get** /minindex | Minimum index
[**GetTimeSeriesMinMax**](TechnicalIndicatorAPI.md#GetTimeSeriesMinMax) | **Get** /minmax | Minimum and maximum
[**GetTimeSeriesMinMaxIndex**](TechnicalIndicatorAPI.md#GetTimeSeriesMinMaxIndex) | **Get** /minmaxindex | Minimum and maximum index
[**GetTimeSeriesMinusDI**](TechnicalIndicatorAPI.md#GetTimeSeriesMinusDI) | **Get** /minus_di | Minus directional indicator
[**GetTimeSeriesMinusDM**](TechnicalIndicatorAPI.md#GetTimeSeriesMinusDM) | **Get** /minus_dm | Minus directional movement
[**GetTimeSeriesMom**](TechnicalIndicatorAPI.md#GetTimeSeriesMom) | **Get** /mom | Momentum
[**GetTimeSeriesMult**](TechnicalIndicatorAPI.md#GetTimeSeriesMult) | **Get** /mult | Multiplication
[**GetTimeSeriesNatr**](TechnicalIndicatorAPI.md#GetTimeSeriesNatr) | **Get** /natr | Normalized average true range
[**GetTimeSeriesObv**](TechnicalIndicatorAPI.md#GetTimeSeriesObv) | **Get** /obv | On balance volume
[**GetTimeSeriesPercentB**](TechnicalIndicatorAPI.md#GetTimeSeriesPercentB) | **Get** /percent_b | Percent B
[**GetTimeSeriesPivotPointsHL**](TechnicalIndicatorAPI.md#GetTimeSeriesPivotPointsHL) | **Get** /pivot_points_hl | Pivot points high low
[**GetTimeSeriesPlusDI**](TechnicalIndicatorAPI.md#GetTimeSeriesPlusDI) | **Get** /plus_di | Plus directional indicator
[**GetTimeSeriesPlusDM**](TechnicalIndicatorAPI.md#GetTimeSeriesPlusDM) | **Get** /plus_dm | Plus directional movement
[**GetTimeSeriesPpo**](TechnicalIndicatorAPI.md#GetTimeSeriesPpo) | **Get** /ppo | Percentage price oscillator
[**GetTimeSeriesRoc**](TechnicalIndicatorAPI.md#GetTimeSeriesRoc) | **Get** /roc | Rate of change
[**GetTimeSeriesRocp**](TechnicalIndicatorAPI.md#GetTimeSeriesRocp) | **Get** /rocp | Rate of change percentage
[**GetTimeSeriesRocr**](TechnicalIndicatorAPI.md#GetTimeSeriesRocr) | **Get** /rocr | Rate of change ratio
[**GetTimeSeriesRocr100**](TechnicalIndicatorAPI.md#GetTimeSeriesRocr100) | **Get** /rocr100 | Rate of change ratio 100
[**GetTimeSeriesRsi**](TechnicalIndicatorAPI.md#GetTimeSeriesRsi) | **Get** /rsi | Relative strength index
[**GetTimeSeriesRvol**](TechnicalIndicatorAPI.md#GetTimeSeriesRvol) | **Get** /rvol | Relative volume
[**GetTimeSeriesSar**](TechnicalIndicatorAPI.md#GetTimeSeriesSar) | **Get** /sar | Parabolic stop and reverse
[**GetTimeSeriesSarExt**](TechnicalIndicatorAPI.md#GetTimeSeriesSarExt) | **Get** /sarext | Parabolic stop and reverse extended
[**GetTimeSeriesSma**](TechnicalIndicatorAPI.md#GetTimeSeriesSma) | **Get** /sma | Simple moving average
[**GetTimeSeriesSqrt**](TechnicalIndicatorAPI.md#GetTimeSeriesSqrt) | **Get** /sqrt | Square root
[**GetTimeSeriesStdDev**](TechnicalIndicatorAPI.md#GetTimeSeriesStdDev) | **Get** /stddev | Standard deviation
[**GetTimeSeriesStoch**](TechnicalIndicatorAPI.md#GetTimeSeriesStoch) | **Get** /stoch | Stochastic oscillator
[**GetTimeSeriesStochF**](TechnicalIndicatorAPI.md#GetTimeSeriesStochF) | **Get** /stochf | Stochastic fast
[**GetTimeSeriesStochRsi**](TechnicalIndicatorAPI.md#GetTimeSeriesStochRsi) | **Get** /stochrsi | Stochastic relative strength index
[**GetTimeSeriesSub**](TechnicalIndicatorAPI.md#GetTimeSeriesSub) | **Get** /sub | Subtraction
[**GetTimeSeriesSum**](TechnicalIndicatorAPI.md#GetTimeSeriesSum) | **Get** /sum | Summation
[**GetTimeSeriesSuperTrend**](TechnicalIndicatorAPI.md#GetTimeSeriesSuperTrend) | **Get** /supertrend | Supertrend
[**GetTimeSeriesSuperTrendHeikinAshiCandles**](TechnicalIndicatorAPI.md#GetTimeSeriesSuperTrendHeikinAshiCandles) | **Get** /supertrend_heikinashicandles | Supertrend Heikin Ashi candles
[**GetTimeSeriesT3ma**](TechnicalIndicatorAPI.md#GetTimeSeriesT3ma) | **Get** /t3ma | Triple exponential moving average
[**GetTimeSeriesTRange**](TechnicalIndicatorAPI.md#GetTimeSeriesTRange) | **Get** /trange | True range
[**GetTimeSeriesTema**](TechnicalIndicatorAPI.md#GetTimeSeriesTema) | **Get** /tema | Triple exponential moving average
[**GetTimeSeriesTrima**](TechnicalIndicatorAPI.md#GetTimeSeriesTrima) | **Get** /trima | Triangular moving average
[**GetTimeSeriesTsf**](TechnicalIndicatorAPI.md#GetTimeSeriesTsf) | **Get** /tsf | Time series forecast
[**GetTimeSeriesTypPrice**](TechnicalIndicatorAPI.md#GetTimeSeriesTypPrice) | **Get** /typprice | Typical price
[**GetTimeSeriesUltOsc**](TechnicalIndicatorAPI.md#GetTimeSeriesUltOsc) | **Get** /ultosc | Ultimate oscillator endpoint
[**GetTimeSeriesVar**](TechnicalIndicatorAPI.md#GetTimeSeriesVar) | **Get** /var | Variance
[**GetTimeSeriesVwap**](TechnicalIndicatorAPI.md#GetTimeSeriesVwap) | **Get** /vwap | Volume weighted average price
[**GetTimeSeriesWclPrice**](TechnicalIndicatorAPI.md#GetTimeSeriesWclPrice) | **Get** /wclprice | Weighted close price
[**GetTimeSeriesWillR**](TechnicalIndicatorAPI.md#GetTimeSeriesWillR) | **Get** /willr | Williams %R
[**GetTimeSeriesWma**](TechnicalIndicatorAPI.md#GetTimeSeriesWma) | **Get** /wma | Weighted moving average



## GetTimeSeriesAd

> GetTimeSeriesAd200Response GetTimeSeriesAd(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).IncludeOhlc(includeOhlc).Execute()

Accumulation/distribution



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesAd(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesAd``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesAd`: GetTimeSeriesAd200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesAd`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesAdRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesAd200Response**](GetTimeSeriesAd200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesAdOsc

> GetTimeSeriesAdOsc200Response GetTimeSeriesAdOsc(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).FastPeriod(fastPeriod).SlowPeriod(slowPeriod).IncludeOhlc(includeOhlc).Execute()

Accumulation/distribution oscillator



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	fastPeriod := int64(789) // int64 | Number of periods for fast moving average. Takes values in the range from `1` to `800` (optional) (default to 12)
	slowPeriod := int64(789) // int64 | Number of periods for slow moving average. Takes values in the range from `1` to `800` (optional) (default to 26)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesAdOsc(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).FastPeriod(fastPeriod).SlowPeriod(slowPeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesAdOsc``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesAdOsc`: GetTimeSeriesAdOsc200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesAdOsc`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesAdOscRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **fastPeriod** | **int64** | Number of periods for fast moving average. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 12]
 **slowPeriod** | **int64** | Number of periods for slow moving average. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 26]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesAdOsc200Response**](GetTimeSeriesAdOsc200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesAdd

> GetTimeSeriesAdd200Response GetTimeSeriesAdd(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType1(seriesType1).SeriesType2(seriesType2).IncludeOhlc(includeOhlc).Execute()

Addition



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType1 := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type used as the first part of technical indicator (optional) (default to "open")
	seriesType2 := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type used as the second part of technical indicator (optional) (default to "open")
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesAdd(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType1(seriesType1).SeriesType2(seriesType2).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesAdd``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesAdd`: GetTimeSeriesAdd200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesAdd`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesAddRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType1** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type used as the first part of technical indicator | [default to &quot;open&quot;]
 **seriesType2** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type used as the second part of technical indicator | [default to &quot;open&quot;]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesAdd200Response**](GetTimeSeriesAdd200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesAdx

> GetTimeSeriesAdx200Response GetTimeSeriesAdx(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Average directional index



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 14)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesAdx(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesAdx``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesAdx`: GetTimeSeriesAdx200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesAdx`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesAdxRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 14]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesAdx200Response**](GetTimeSeriesAdx200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesAdxr

> GetTimeSeriesAdxr200Response GetTimeSeriesAdxr(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Average directional movement index rating



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 14)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesAdxr(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesAdxr``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesAdxr`: GetTimeSeriesAdxr200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesAdxr`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesAdxrRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 14]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesAdxr200Response**](GetTimeSeriesAdxr200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesApo

> GetTimeSeriesApo200Response GetTimeSeriesApo(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).FastPeriod(fastPeriod).SlowPeriod(slowPeriod).MaType(maType).IncludeOhlc(includeOhlc).Execute()

Absolute price oscillator



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	fastPeriod := int64(789) // int64 | Number of periods for fast moving average. Takes values in the range from `1` to `800` (optional) (default to 12)
	slowPeriod := int64(789) // int64 | Number of periods for slow moving average. Takes values in the range from `1` to `800` (optional) (default to 26)
	maType := openapiclient.MaTypeEnum("SMA") // MaTypeEnum | The type of moving average used (optional) (default to "SMA")
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesApo(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).FastPeriod(fastPeriod).SlowPeriod(slowPeriod).MaType(maType).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesApo``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesApo`: GetTimeSeriesApo200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesApo`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesApoRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **fastPeriod** | **int64** | Number of periods for fast moving average. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 12]
 **slowPeriod** | **int64** | Number of periods for slow moving average. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 26]
 **maType** | [**MaTypeEnum**](MaTypeEnum.md) | The type of moving average used | [default to &quot;SMA&quot;]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesApo200Response**](GetTimeSeriesApo200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesAroon

> GetTimeSeriesAroon200Response GetTimeSeriesAroon(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Aroon indicator



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 14)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesAroon(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesAroon``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesAroon`: GetTimeSeriesAroon200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesAroon`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesAroonRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 14]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesAroon200Response**](GetTimeSeriesAroon200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesAroonOsc

> GetTimeSeriesAroonOsc200Response GetTimeSeriesAroonOsc(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Aroon oscillator



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 14)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesAroonOsc(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesAroonOsc``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesAroonOsc`: GetTimeSeriesAroonOsc200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesAroonOsc`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesAroonOscRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 14]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesAroonOsc200Response**](GetTimeSeriesAroonOsc200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesAtr

> GetTimeSeriesAtr200Response GetTimeSeriesAtr(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Average true range



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 14)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesAtr(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesAtr``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesAtr`: GetTimeSeriesAtr200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesAtr`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesAtrRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 14]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesAtr200Response**](GetTimeSeriesAtr200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesAvg

> GetTimeSeriesAvg200Response GetTimeSeriesAvg(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Average



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesAvg(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesAvg``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesAvg`: GetTimeSeriesAvg200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesAvg`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesAvgRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesAvg200Response**](GetTimeSeriesAvg200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesAvgPrice

> GetTimeSeriesAvgPrice200Response GetTimeSeriesAvgPrice(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).IncludeOhlc(includeOhlc).Execute()

Average price



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesAvgPrice(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesAvgPrice``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesAvgPrice`: GetTimeSeriesAvgPrice200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesAvgPrice`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesAvgPriceRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesAvgPrice200Response**](GetTimeSeriesAvgPrice200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesBBands

> GetTimeSeriesBBands200Response GetTimeSeriesBBands(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).Sd(sd).MaType(maType).IncludeOhlc(includeOhlc).Execute()

Bollinger bands



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 20)
	sd := float64(1.2) // float64 | Number of standard deviations. Must be at least `1` (optional) (default to 2)
	maType := openapiclient.MaTypeEnum("SMA") // MaTypeEnum | The type of moving average used (optional) (default to "SMA")
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesBBands(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).Sd(sd).MaType(maType).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesBBands``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesBBands`: GetTimeSeriesBBands200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesBBands`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesBBandsRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 20]
 **sd** | **float64** | Number of standard deviations. Must be at least &#x60;1&#x60; | [default to 2]
 **maType** | [**MaTypeEnum**](MaTypeEnum.md) | The type of moving average used | [default to &quot;SMA&quot;]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesBBands200Response**](GetTimeSeriesBBands200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesBeta

> GetTimeSeriesBeta200Response GetTimeSeriesBeta(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType1(seriesType1).SeriesType2(seriesType2).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Beta indicator



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType1 := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type used as the first part of technical indicator (optional) (default to "open")
	seriesType2 := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type used as the second part of technical indicator (optional) (default to "open")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesBeta(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType1(seriesType1).SeriesType2(seriesType2).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesBeta``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesBeta`: GetTimeSeriesBeta200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesBeta`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesBetaRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType1** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type used as the first part of technical indicator | [default to &quot;open&quot;]
 **seriesType2** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type used as the second part of technical indicator | [default to &quot;open&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesBeta200Response**](GetTimeSeriesBeta200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesBop

> GetTimeSeriesBop200Response GetTimeSeriesBop(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).IncludeOhlc(includeOhlc).Execute()

Balance of power



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesBop(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesBop``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesBop`: GetTimeSeriesBop200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesBop`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesBopRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesBop200Response**](GetTimeSeriesBop200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesCci

> GetTimeSeriesCci200Response GetTimeSeriesCci(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Commodity channel index



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 20)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesCci(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesCci``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesCci`: GetTimeSeriesCci200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesCci`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesCciRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 20]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesCci200Response**](GetTimeSeriesCci200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesCeil

> GetTimeSeriesCeil200Response GetTimeSeriesCeil(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).IncludeOhlc(includeOhlc).Execute()

Ceiling



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesCeil(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesCeil``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesCeil`: GetTimeSeriesCeil200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesCeil`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesCeilRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesCeil200Response**](GetTimeSeriesCeil200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesCmo

> GetTimeSeriesCmo200Response GetTimeSeriesCmo(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Chande momentum oscillator



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesCmo(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesCmo``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesCmo`: GetTimeSeriesCmo200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesCmo`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesCmoRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesCmo200Response**](GetTimeSeriesCmo200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesCoppock

> GetTimeSeriesCoppock200Response GetTimeSeriesCoppock(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).WmaPeriod(wmaPeriod).LongRocPeriod(longRocPeriod).ShortRocPeriod(shortRocPeriod).IncludeOhlc(includeOhlc).Execute()

Coppock curve



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	wmaPeriod := int64(789) // int64 | Number of periods for weighted moving average. Takes values in the range from `1` to `800` (optional) (default to 10)
	longRocPeriod := int64(789) // int64 | Number of periods for long term rate of change. Takes values in the range from `1` to `800` (optional) (default to 14)
	shortRocPeriod := int64(789) // int64 | Number of periods for short term rate of change. Takes values in the range from `1` to `800` (optional) (default to 11)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesCoppock(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).WmaPeriod(wmaPeriod).LongRocPeriod(longRocPeriod).ShortRocPeriod(shortRocPeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesCoppock``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesCoppock`: GetTimeSeriesCoppock200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesCoppock`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesCoppockRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **wmaPeriod** | **int64** | Number of periods for weighted moving average. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 10]
 **longRocPeriod** | **int64** | Number of periods for long term rate of change. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 14]
 **shortRocPeriod** | **int64** | Number of periods for short term rate of change. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 11]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesCoppock200Response**](GetTimeSeriesCoppock200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesCorrel

> GetTimeSeriesCorrel200Response GetTimeSeriesCorrel(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType1(seriesType1).SeriesType2(seriesType2).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Correlation



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType1 := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type used as the first part of technical indicator (optional) (default to "open")
	seriesType2 := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type used as the second part of technical indicator (optional) (default to "open")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesCorrel(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType1(seriesType1).SeriesType2(seriesType2).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesCorrel``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesCorrel`: GetTimeSeriesCorrel200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesCorrel`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesCorrelRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType1** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type used as the first part of technical indicator | [default to &quot;open&quot;]
 **seriesType2** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type used as the second part of technical indicator | [default to &quot;open&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesCorrel200Response**](GetTimeSeriesCorrel200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesCrsi

> GetTimeSeriesCrsi200Response GetTimeSeriesCrsi(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).RsiPeriod(rsiPeriod).UpDownLength(upDownLength).PercentRankPeriod(percentRankPeriod).IncludeOhlc(includeOhlc).Execute()

Connors relative strength index



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	rsiPeriod := int64(789) // int64 | Number of periods for RSI used to calculate price momentum. Takes values in the range from `1` to `800` (optional) (default to 3)
	upDownLength := int64(789) // int64 | Number of periods for RSI used to calculate up/down trend. Takes values in the range from `1` to `800` (optional) (default to 2)
	percentRankPeriod := int64(789) // int64 | Number of periods used to calculate PercentRank. Takes values in the range from `1` to `800` (optional) (default to 100)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesCrsi(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).RsiPeriod(rsiPeriod).UpDownLength(upDownLength).PercentRankPeriod(percentRankPeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesCrsi``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesCrsi`: GetTimeSeriesCrsi200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesCrsi`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesCrsiRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **rsiPeriod** | **int64** | Number of periods for RSI used to calculate price momentum. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 3]
 **upDownLength** | **int64** | Number of periods for RSI used to calculate up/down trend. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 2]
 **percentRankPeriod** | **int64** | Number of periods used to calculate PercentRank. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 100]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesCrsi200Response**](GetTimeSeriesCrsi200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesDema

> GetTimeSeriesDema200Response GetTimeSeriesDema(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Double exponential moving average



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesDema(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesDema``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesDema`: GetTimeSeriesDema200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesDema`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesDemaRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesDema200Response**](GetTimeSeriesDema200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesDiv

> GetTimeSeriesDiv200Response GetTimeSeriesDiv(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType1(seriesType1).SeriesType2(seriesType2).IncludeOhlc(includeOhlc).Execute()

Division



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType1 := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type used as the first part of technical indicator (optional) (default to "open")
	seriesType2 := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type used as the second part of technical indicator (optional) (default to "open")
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesDiv(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType1(seriesType1).SeriesType2(seriesType2).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesDiv``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesDiv`: GetTimeSeriesDiv200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesDiv`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesDivRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType1** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type used as the first part of technical indicator | [default to &quot;open&quot;]
 **seriesType2** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type used as the second part of technical indicator | [default to &quot;open&quot;]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesDiv200Response**](GetTimeSeriesDiv200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesDpo

> GetTimeSeriesDpo200Response GetTimeSeriesDpo(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).Centered(centered).IncludeOhlc(includeOhlc).Execute()

Detrended price oscillator



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	centered := true // bool | Specifies if there should be a shift to match the current price (optional) (default to false)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesDpo(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).Centered(centered).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesDpo``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesDpo`: GetTimeSeriesDpo200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesDpo`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesDpoRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **centered** | **bool** | Specifies if there should be a shift to match the current price | [default to false]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesDpo200Response**](GetTimeSeriesDpo200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesDx

> GetTimeSeriesDx200Response GetTimeSeriesDx(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Directional movement index



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 14)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesDx(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesDx``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesDx`: GetTimeSeriesDx200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesDx`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesDxRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 14]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesDx200Response**](GetTimeSeriesDx200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesEma

> GetTimeSeriesEma200Response GetTimeSeriesEma(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Exponential moving average



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesEma(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesEma``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesEma`: GetTimeSeriesEma200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesEma`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesEmaRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesEma200Response**](GetTimeSeriesEma200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesExp

> GetTimeSeriesExp200Response GetTimeSeriesExp(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).IncludeOhlc(includeOhlc).Execute()

Exponential



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesExp(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesExp``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesExp`: GetTimeSeriesExp200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesExp`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesExpRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesExp200Response**](GetTimeSeriesExp200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesFloor

> GetTimeSeriesFloor200Response GetTimeSeriesFloor(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).IncludeOhlc(includeOhlc).Execute()

Floor



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesFloor(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesFloor``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesFloor`: GetTimeSeriesFloor200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesFloor`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesFloorRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesFloor200Response**](GetTimeSeriesFloor200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesHeikinashiCandles

> GetTimeSeriesHeikinashiCandles200Response GetTimeSeriesHeikinashiCandles(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).IncludeOhlc(includeOhlc).Execute()

Heikinashi candles



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesHeikinashiCandles(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesHeikinashiCandles``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesHeikinashiCandles`: GetTimeSeriesHeikinashiCandles200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesHeikinashiCandles`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesHeikinashiCandlesRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesHeikinashiCandles200Response**](GetTimeSeriesHeikinashiCandles200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesHlc3

> GetTimeSeriesHlc3200Response GetTimeSeriesHlc3(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).IncludeOhlc(includeOhlc).Execute()

High, low, close average



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesHlc3(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesHlc3``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesHlc3`: GetTimeSeriesHlc3200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesHlc3`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesHlc3Request struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesHlc3200Response**](GetTimeSeriesHlc3200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesHtDcPeriod

> GetTimeSeriesHtDcPeriod200Response GetTimeSeriesHtDcPeriod(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).IncludeOhlc(includeOhlc).Execute()

Hilbert transform dominant cycle period



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesHtDcPeriod(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesHtDcPeriod``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesHtDcPeriod`: GetTimeSeriesHtDcPeriod200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesHtDcPeriod`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesHtDcPeriodRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesHtDcPeriod200Response**](GetTimeSeriesHtDcPeriod200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesHtDcPhase

> GetTimeSeriesHtDcPhase200Response GetTimeSeriesHtDcPhase(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).IncludeOhlc(includeOhlc).Execute()

Hilbert transform dominant cycle phase



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesHtDcPhase(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesHtDcPhase``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesHtDcPhase`: GetTimeSeriesHtDcPhase200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesHtDcPhase`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesHtDcPhaseRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesHtDcPhase200Response**](GetTimeSeriesHtDcPhase200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesHtPhasor

> GetTimeSeriesHtPhasor200Response GetTimeSeriesHtPhasor(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).IncludeOhlc(includeOhlc).Execute()

Hilbert transform phasor components



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesHtPhasor(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesHtPhasor``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesHtPhasor`: GetTimeSeriesHtPhasor200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesHtPhasor`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesHtPhasorRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesHtPhasor200Response**](GetTimeSeriesHtPhasor200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesHtSine

> GetTimeSeriesHtSine200Response GetTimeSeriesHtSine(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).IncludeOhlc(includeOhlc).Execute()

Hilbert transform sine wave



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesHtSine(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesHtSine``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesHtSine`: GetTimeSeriesHtSine200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesHtSine`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesHtSineRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesHtSine200Response**](GetTimeSeriesHtSine200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesHtTrendMode

> GetTimeSeriesHtTrendMode200Response GetTimeSeriesHtTrendMode(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).IncludeOhlc(includeOhlc).Execute()

Hilbert transform trend vs cycle mode



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesHtTrendMode(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesHtTrendMode``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesHtTrendMode`: GetTimeSeriesHtTrendMode200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesHtTrendMode`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesHtTrendModeRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesHtTrendMode200Response**](GetTimeSeriesHtTrendMode200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesHtTrendline

> GetTimeSeriesHtTrendline200Response GetTimeSeriesHtTrendline(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).IncludeOhlc(includeOhlc).Execute()

Hilbert transform instantaneous trendline



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesHtTrendline(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesHtTrendline``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesHtTrendline`: GetTimeSeriesHtTrendline200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesHtTrendline`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesHtTrendlineRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesHtTrendline200Response**](GetTimeSeriesHtTrendline200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesIchimoku

> GetTimeSeriesIchimoku200Response GetTimeSeriesIchimoku(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).ConversionLinePeriod(conversionLinePeriod).BaseLinePeriod(baseLinePeriod).LeadingSpanBPeriod(leadingSpanBPeriod).LaggingSpanPeriod(laggingSpanPeriod).IncludeAheadSpanPeriod(includeAheadSpanPeriod).IncludeOhlc(includeOhlc).Execute()

Ichimoku cloud



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	conversionLinePeriod := int64(789) // int64 | The time period used for generating the conversation line. Takes values in the range from `1` to `800` (optional) (default to 9)
	baseLinePeriod := int64(789) // int64 | The time period used for generating the base line. Takes values in the range from `1` to `800` (optional) (default to 26)
	leadingSpanBPeriod := int64(789) // int64 | The time period used for generating the leading span B line. Takes values in the range from `1` to `800` (optional) (default to 52)
	laggingSpanPeriod := int64(789) // int64 | The time period used for generating the lagging span line. Takes values in the range from `1` to `800` (optional) (default to 26)
	includeAheadSpanPeriod := true // bool | Indicates whether to include ahead span period (optional) (default to true)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesIchimoku(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).ConversionLinePeriod(conversionLinePeriod).BaseLinePeriod(baseLinePeriod).LeadingSpanBPeriod(leadingSpanBPeriod).LaggingSpanPeriod(laggingSpanPeriod).IncludeAheadSpanPeriod(includeAheadSpanPeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesIchimoku``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesIchimoku`: GetTimeSeriesIchimoku200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesIchimoku`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesIchimokuRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **conversionLinePeriod** | **int64** | The time period used for generating the conversation line. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **baseLinePeriod** | **int64** | The time period used for generating the base line. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 26]
 **leadingSpanBPeriod** | **int64** | The time period used for generating the leading span B line. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 52]
 **laggingSpanPeriod** | **int64** | The time period used for generating the lagging span line. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 26]
 **includeAheadSpanPeriod** | **bool** | Indicates whether to include ahead span period | [default to true]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesIchimoku200Response**](GetTimeSeriesIchimoku200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesKama

> GetTimeSeriesKama200Response GetTimeSeriesKama(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Kaufman adaptive moving average



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesKama(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesKama``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesKama`: GetTimeSeriesKama200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesKama`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesKamaRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesKama200Response**](GetTimeSeriesKama200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesKeltner

> GetTimeSeriesKeltner200Response GetTimeSeriesKeltner(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod(timePeriod).AtrTimePeriod(atrTimePeriod).Multiplier(multiplier).SeriesType(seriesType).MaType(maType).IncludeOhlc(includeOhlc).Execute()

Keltner channel



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 20)
	atrTimePeriod := int64(789) // int64 | The time period used for calculating the Average True Range. Takes values in the range from `1` to `800` (optional) (default to 10)
	multiplier := int64(789) // int64 | The factor used to adjust the indicator's sensitivity (optional) (default to 2)
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	maType := openapiclient.MaTypeEnum("SMA") // MaTypeEnum | The type of moving average used (optional) (default to "SMA")
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesKeltner(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod(timePeriod).AtrTimePeriod(atrTimePeriod).Multiplier(multiplier).SeriesType(seriesType).MaType(maType).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesKeltner``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesKeltner`: GetTimeSeriesKeltner200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesKeltner`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesKeltnerRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 20]
 **atrTimePeriod** | **int64** | The time period used for calculating the Average True Range. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 10]
 **multiplier** | **int64** | The factor used to adjust the indicator&#39;s sensitivity | [default to 2]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **maType** | [**MaTypeEnum**](MaTypeEnum.md) | The type of moving average used | [default to &quot;SMA&quot;]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesKeltner200Response**](GetTimeSeriesKeltner200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesKst

> GetTimeSeriesKst200Response GetTimeSeriesKst(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).RocPeriod1(rocPeriod1).RocPeriod2(rocPeriod2).RocPeriod3(rocPeriod3).RocPeriod4(rocPeriod4).SmaPeriod1(smaPeriod1).SmaPeriod2(smaPeriod2).SmaPeriod3(smaPeriod3).SmaPeriod4(smaPeriod4).SignalPeriod(signalPeriod).IncludeOhlc(includeOhlc).Execute()

Know sure thing



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	rocPeriod1 := int64(789) // int64 | The time period for the first Rate of Change calculation. (optional) (default to 10)
	rocPeriod2 := int64(789) // int64 | The time period for the second Rate of Change calculation. (optional) (default to 15)
	rocPeriod3 := int64(789) // int64 | The time period for the third Rate of Change calculation. (optional) (default to 20)
	rocPeriod4 := int64(789) // int64 | The time period for the forth Rate of Change calculation. (optional) (default to 30)
	smaPeriod1 := int64(789) // int64 | The time period for the first Simple Moving Average. (optional) (default to 10)
	smaPeriod2 := int64(789) // int64 | The time period for the second Simple Moving Average. (optional) (default to 10)
	smaPeriod3 := int64(789) // int64 | The time period for the third Simple Moving Average. (optional) (default to 10)
	smaPeriod4 := int64(789) // int64 | The time period for the forth Simple Moving Average. (optional) (default to 15)
	signalPeriod := int64(789) // int64 | The time period used for generating the signal line. (optional) (default to 9)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesKst(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).RocPeriod1(rocPeriod1).RocPeriod2(rocPeriod2).RocPeriod3(rocPeriod3).RocPeriod4(rocPeriod4).SmaPeriod1(smaPeriod1).SmaPeriod2(smaPeriod2).SmaPeriod3(smaPeriod3).SmaPeriod4(smaPeriod4).SignalPeriod(signalPeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesKst``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesKst`: GetTimeSeriesKst200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesKst`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesKstRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **rocPeriod1** | **int64** | The time period for the first Rate of Change calculation. | [default to 10]
 **rocPeriod2** | **int64** | The time period for the second Rate of Change calculation. | [default to 15]
 **rocPeriod3** | **int64** | The time period for the third Rate of Change calculation. | [default to 20]
 **rocPeriod4** | **int64** | The time period for the forth Rate of Change calculation. | [default to 30]
 **smaPeriod1** | **int64** | The time period for the first Simple Moving Average. | [default to 10]
 **smaPeriod2** | **int64** | The time period for the second Simple Moving Average. | [default to 10]
 **smaPeriod3** | **int64** | The time period for the third Simple Moving Average. | [default to 10]
 **smaPeriod4** | **int64** | The time period for the forth Simple Moving Average. | [default to 15]
 **signalPeriod** | **int64** | The time period used for generating the signal line. | [default to 9]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesKst200Response**](GetTimeSeriesKst200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesLinearReg

> GetTimeSeriesLinearReg200Response GetTimeSeriesLinearReg(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Linear regression



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesLinearReg(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesLinearReg``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesLinearReg`: GetTimeSeriesLinearReg200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesLinearReg`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesLinearRegRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesLinearReg200Response**](GetTimeSeriesLinearReg200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesLinearRegAngle

> GetTimeSeriesLinearRegAngle200Response GetTimeSeriesLinearRegAngle(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Linear regression angle



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesLinearRegAngle(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesLinearRegAngle``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesLinearRegAngle`: GetTimeSeriesLinearRegAngle200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesLinearRegAngle`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesLinearRegAngleRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesLinearRegAngle200Response**](GetTimeSeriesLinearRegAngle200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesLinearRegIntercept

> GetTimeSeriesLinearRegIntercept200Response GetTimeSeriesLinearRegIntercept(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Linear regression intercept



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesLinearRegIntercept(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesLinearRegIntercept``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesLinearRegIntercept`: GetTimeSeriesLinearRegIntercept200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesLinearRegIntercept`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesLinearRegInterceptRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesLinearRegIntercept200Response**](GetTimeSeriesLinearRegIntercept200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesLinearRegSlope

> GetTimeSeriesLinearRegSlope200Response GetTimeSeriesLinearRegSlope(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Linear regression slope



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesLinearRegSlope(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesLinearRegSlope``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesLinearRegSlope`: GetTimeSeriesLinearRegSlope200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesLinearRegSlope`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesLinearRegSlopeRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesLinearRegSlope200Response**](GetTimeSeriesLinearRegSlope200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesLn

> GetTimeSeriesLn200Response GetTimeSeriesLn(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).IncludeOhlc(includeOhlc).Execute()

Natural logarithm



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesLn(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesLn``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesLn`: GetTimeSeriesLn200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesLn`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesLnRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesLn200Response**](GetTimeSeriesLn200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesLog10

> GetTimeSeriesLog10200Response GetTimeSeriesLog10(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).IncludeOhlc(includeOhlc).Execute()

Base-10 logarithm



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesLog10(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesLog10``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesLog10`: GetTimeSeriesLog10200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesLog10`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesLog10Request struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesLog10200Response**](GetTimeSeriesLog10200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesMa

> GetTimeSeriesMa200Response GetTimeSeriesMa(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).MaType(maType).IncludeOhlc(includeOhlc).Execute()

Moving average



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	maType := openapiclient.MaTypeEnum("SMA") // MaTypeEnum | The type of moving average used (optional) (default to "SMA")
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesMa(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).MaType(maType).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesMa``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesMa`: GetTimeSeriesMa200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesMa`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesMaRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **maType** | [**MaTypeEnum**](MaTypeEnum.md) | The type of moving average used | [default to &quot;SMA&quot;]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesMa200Response**](GetTimeSeriesMa200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesMacd

> GetTimeSeriesMacd200Response GetTimeSeriesMacd(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).FastPeriod(fastPeriod).SlowPeriod(slowPeriod).SignalPeriod(signalPeriod).IncludeOhlc(includeOhlc).Execute()

Moving average convergence divergence



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	fastPeriod := int64(789) // int64 | Number of periods for fast moving average. Takes values in the range from `1` to `800` (optional) (default to 12)
	slowPeriod := int64(789) // int64 | Number of periods for slow moving average. Takes values in the range from `1` to `800` (optional) (default to 26)
	signalPeriod := int64(789) // int64 | The time period used for generating the signal line. (optional) (default to 9)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesMacd(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).FastPeriod(fastPeriod).SlowPeriod(slowPeriod).SignalPeriod(signalPeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesMacd``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesMacd`: GetTimeSeriesMacd200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesMacd`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesMacdRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **fastPeriod** | **int64** | Number of periods for fast moving average. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 12]
 **slowPeriod** | **int64** | Number of periods for slow moving average. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 26]
 **signalPeriod** | **int64** | The time period used for generating the signal line. | [default to 9]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesMacd200Response**](GetTimeSeriesMacd200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesMacdExt

> GetTimeSeriesMacdExt200Response GetTimeSeriesMacdExt(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).FastPeriod(fastPeriod).FastMaType(fastMaType).SlowPeriod(slowPeriod).SlowMaType(slowMaType).SignalPeriod(signalPeriod).SignalMaType(signalMaType).IncludeOhlc(includeOhlc).Execute()

Moving average convergence divergence extension



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	fastPeriod := int64(789) // int64 | Number of periods for fast moving average. Takes values in the range from `1` to `800` (optional) (default to 12)
	fastMaType := openapiclient.MaTypeEnum("SMA") // MaTypeEnum | The type of fast moving average used in the calculation. (optional) (default to "SMA")
	slowPeriod := int64(789) // int64 | Number of periods for slow moving average. Takes values in the range from `1` to `800` (optional) (default to 26)
	slowMaType := openapiclient.MaTypeEnum("SMA") // MaTypeEnum | The type of slow moving average used in the calculation. (optional) (default to "SMA")
	signalPeriod := int64(789) // int64 | The time period used for generating the signal line. (optional) (default to 9)
	signalMaType := openapiclient.MaTypeEnum("SMA") // MaTypeEnum | The type of fast moving average used for generating the signal line. (optional) (default to "SMA")
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesMacdExt(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).FastPeriod(fastPeriod).FastMaType(fastMaType).SlowPeriod(slowPeriod).SlowMaType(slowMaType).SignalPeriod(signalPeriod).SignalMaType(signalMaType).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesMacdExt``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesMacdExt`: GetTimeSeriesMacdExt200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesMacdExt`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesMacdExtRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **fastPeriod** | **int64** | Number of periods for fast moving average. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 12]
 **fastMaType** | [**MaTypeEnum**](MaTypeEnum.md) | The type of fast moving average used in the calculation. | [default to &quot;SMA&quot;]
 **slowPeriod** | **int64** | Number of periods for slow moving average. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 26]
 **slowMaType** | [**MaTypeEnum**](MaTypeEnum.md) | The type of slow moving average used in the calculation. | [default to &quot;SMA&quot;]
 **signalPeriod** | **int64** | The time period used for generating the signal line. | [default to 9]
 **signalMaType** | [**MaTypeEnum**](MaTypeEnum.md) | The type of fast moving average used for generating the signal line. | [default to &quot;SMA&quot;]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesMacdExt200Response**](GetTimeSeriesMacdExt200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesMacdSlope

> GetTimeSeriesMacdSlope200Response GetTimeSeriesMacdSlope(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).FastPeriod(fastPeriod).SlowPeriod(slowPeriod).SignalPeriod(signalPeriod).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Moving average convergence divergence slope



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	fastPeriod := int64(789) // int64 | Number of periods for fast moving average. Takes values in the range from `1` to `800` (optional) (default to 12)
	slowPeriod := int64(789) // int64 | Number of periods for slow moving average. Takes values in the range from `1` to `800` (optional) (default to 26)
	signalPeriod := int64(789) // int64 | The time period used for generating the signal line. (optional) (default to 9)
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesMacdSlope(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).FastPeriod(fastPeriod).SlowPeriod(slowPeriod).SignalPeriod(signalPeriod).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesMacdSlope``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesMacdSlope`: GetTimeSeriesMacdSlope200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesMacdSlope`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesMacdSlopeRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **fastPeriod** | **int64** | Number of periods for fast moving average. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 12]
 **slowPeriod** | **int64** | Number of periods for slow moving average. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 26]
 **signalPeriod** | **int64** | The time period used for generating the signal line. | [default to 9]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesMacdSlope200Response**](GetTimeSeriesMacdSlope200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesMama

> GetTimeSeriesMama200Response GetTimeSeriesMama(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).FastLimit(fastLimit).SlowLimit(slowLimit).IncludeOhlc(includeOhlc).Execute()

MESA adaptive moving average



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	fastLimit := float64(1.2) // float64 | The limit for the fast moving average. (optional) (default to 0.5)
	slowLimit := float64(1.2) // float64 | The limit for the slow moving average. (optional) (default to 0.05)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesMama(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).FastLimit(fastLimit).SlowLimit(slowLimit).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesMama``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesMama`: GetTimeSeriesMama200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesMama`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesMamaRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **fastLimit** | **float64** | The limit for the fast moving average. | [default to 0.5]
 **slowLimit** | **float64** | The limit for the slow moving average. | [default to 0.05]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesMama200Response**](GetTimeSeriesMama200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesMax

> GetTimeSeriesMax200Response GetTimeSeriesMax(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Maximum



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesMax(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesMax``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesMax`: GetTimeSeriesMax200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesMax`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesMaxRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesMax200Response**](GetTimeSeriesMax200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesMaxIndex

> GetTimeSeriesMaxIndex200Response GetTimeSeriesMaxIndex(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Maximum Index



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesMaxIndex(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesMaxIndex``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesMaxIndex`: GetTimeSeriesMaxIndex200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesMaxIndex`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesMaxIndexRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesMaxIndex200Response**](GetTimeSeriesMaxIndex200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesMcGinleyDynamic

> GetTimeSeriesMcGinleyDynamic200Response GetTimeSeriesMcGinleyDynamic(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

McGinley dynamic indicator



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 14)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesMcGinleyDynamic(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesMcGinleyDynamic``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesMcGinleyDynamic`: GetTimeSeriesMcGinleyDynamic200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesMcGinleyDynamic`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesMcGinleyDynamicRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 14]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesMcGinleyDynamic200Response**](GetTimeSeriesMcGinleyDynamic200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesMedPrice

> GetTimeSeriesMedPrice200Response GetTimeSeriesMedPrice(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).IncludeOhlc(includeOhlc).Execute()

Median price



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesMedPrice(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesMedPrice``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesMedPrice`: GetTimeSeriesMedPrice200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesMedPrice`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesMedPriceRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesMedPrice200Response**](GetTimeSeriesMedPrice200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesMfi

> GetTimeSeriesMfi200Response GetTimeSeriesMfi(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Money flow index



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 14)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesMfi(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesMfi``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesMfi`: GetTimeSeriesMfi200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesMfi`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesMfiRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 14]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesMfi200Response**](GetTimeSeriesMfi200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesMidPoint

> GetTimeSeriesMidPoint200Response GetTimeSeriesMidPoint(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Midpoint



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesMidPoint(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesMidPoint``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesMidPoint`: GetTimeSeriesMidPoint200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesMidPoint`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesMidPointRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesMidPoint200Response**](GetTimeSeriesMidPoint200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesMidPrice

> GetTimeSeriesMidPrice200Response GetTimeSeriesMidPrice(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Midprice



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesMidPrice(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesMidPrice``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesMidPrice`: GetTimeSeriesMidPrice200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesMidPrice`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesMidPriceRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesMidPrice200Response**](GetTimeSeriesMidPrice200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesMin

> GetTimeSeriesMin200Response GetTimeSeriesMin(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Minimum



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesMin(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesMin``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesMin`: GetTimeSeriesMin200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesMin`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesMinRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesMin200Response**](GetTimeSeriesMin200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesMinIndex

> GetTimeSeriesMinIndex200Response GetTimeSeriesMinIndex(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Minimum index



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesMinIndex(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesMinIndex``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesMinIndex`: GetTimeSeriesMinIndex200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesMinIndex`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesMinIndexRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesMinIndex200Response**](GetTimeSeriesMinIndex200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesMinMax

> GetTimeSeriesMinMax200Response GetTimeSeriesMinMax(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Minimum and maximum



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesMinMax(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesMinMax``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesMinMax`: GetTimeSeriesMinMax200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesMinMax`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesMinMaxRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesMinMax200Response**](GetTimeSeriesMinMax200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesMinMaxIndex

> GetTimeSeriesMinMaxIndex200Response GetTimeSeriesMinMaxIndex(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Minimum and maximum index



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesMinMaxIndex(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesMinMaxIndex``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesMinMaxIndex`: GetTimeSeriesMinMaxIndex200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesMinMaxIndex`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesMinMaxIndexRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesMinMaxIndex200Response**](GetTimeSeriesMinMaxIndex200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesMinusDI

> GetTimeSeriesMinusDI200Response GetTimeSeriesMinusDI(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Minus directional indicator



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesMinusDI(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesMinusDI``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesMinusDI`: GetTimeSeriesMinusDI200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesMinusDI`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesMinusDIRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesMinusDI200Response**](GetTimeSeriesMinusDI200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesMinusDM

> GetTimeSeriesMinusDM200Response GetTimeSeriesMinusDM(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Minus directional movement



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesMinusDM(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesMinusDM``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesMinusDM`: GetTimeSeriesMinusDM200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesMinusDM`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesMinusDMRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesMinusDM200Response**](GetTimeSeriesMinusDM200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesMom

> GetTimeSeriesMom200Response GetTimeSeriesMom(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Momentum



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesMom(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesMom``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesMom`: GetTimeSeriesMom200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesMom`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesMomRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesMom200Response**](GetTimeSeriesMom200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesMult

> GetTimeSeriesMult200Response GetTimeSeriesMult(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType1(seriesType1).SeriesType2(seriesType2).IncludeOhlc(includeOhlc).Execute()

Multiplication



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType1 := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type used as the first part of technical indicator (optional) (default to "open")
	seriesType2 := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type used as the second part of technical indicator (optional) (default to "open")
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesMult(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType1(seriesType1).SeriesType2(seriesType2).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesMult``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesMult`: GetTimeSeriesMult200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesMult`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesMultRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType1** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type used as the first part of technical indicator | [default to &quot;open&quot;]
 **seriesType2** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type used as the second part of technical indicator | [default to &quot;open&quot;]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesMult200Response**](GetTimeSeriesMult200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesNatr

> GetTimeSeriesNatr200Response GetTimeSeriesNatr(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Normalized average true range



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 14)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesNatr(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesNatr``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesNatr`: GetTimeSeriesNatr200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesNatr`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesNatrRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 14]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesNatr200Response**](GetTimeSeriesNatr200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesObv

> GetTimeSeriesObv200Response GetTimeSeriesObv(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).IncludeOhlc(includeOhlc).Execute()

On balance volume



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesObv(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesObv``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesObv`: GetTimeSeriesObv200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesObv`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesObvRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesObv200Response**](GetTimeSeriesObv200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesPercentB

> GetTimeSeriesPercentB200Response GetTimeSeriesPercentB(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).Sd(sd).MaType(maType).IncludeOhlc(includeOhlc).Execute()

Percent B



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	sd := float64(1.2) // float64 | The standard deviation applied in the calculation.  Must be at least `1` (optional) (default to 2)
	maType := openapiclient.MaTypeEnum("SMA") // MaTypeEnum | The type of moving average used (optional) (default to "SMA")
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesPercentB(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).Sd(sd).MaType(maType).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesPercentB``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesPercentB`: GetTimeSeriesPercentB200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesPercentB`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesPercentBRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **sd** | **float64** | The standard deviation applied in the calculation.  Must be at least &#x60;1&#x60; | [default to 2]
 **maType** | [**MaTypeEnum**](MaTypeEnum.md) | The type of moving average used | [default to &quot;SMA&quot;]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesPercentB200Response**](GetTimeSeriesPercentB200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesPivotPointsHL

> GetTimeSeriesPivotPointsHL200Response GetTimeSeriesPivotPointsHL(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Pivot points high low



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 10)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesPivotPointsHL(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesPivotPointsHL``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesPivotPointsHL`: GetTimeSeriesPivotPointsHL200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesPivotPointsHL`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesPivotPointsHLRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 10]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesPivotPointsHL200Response**](GetTimeSeriesPivotPointsHL200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesPlusDI

> GetTimeSeriesPlusDI200Response GetTimeSeriesPlusDI(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Plus directional indicator



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesPlusDI(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesPlusDI``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesPlusDI`: GetTimeSeriesPlusDI200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesPlusDI`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesPlusDIRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesPlusDI200Response**](GetTimeSeriesPlusDI200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesPlusDM

> GetTimeSeriesPlusDM200Response GetTimeSeriesPlusDM(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Plus directional movement



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesPlusDM(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesPlusDM``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesPlusDM`: GetTimeSeriesPlusDM200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesPlusDM`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesPlusDMRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesPlusDM200Response**](GetTimeSeriesPlusDM200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesPpo

> GetTimeSeriesPpo200Response GetTimeSeriesPpo(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).FastPeriod(fastPeriod).SlowPeriod(slowPeriod).MaType(maType).IncludeOhlc(includeOhlc).Execute()

Percentage price oscillator



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	fastPeriod := int64(789) // int64 | Number of periods for fast moving average. Takes values in the range from `1` to `800` (optional) (default to 12)
	slowPeriod := int64(789) // int64 | Number of periods for slow moving average. Takes values in the range from `1` to `800` (optional) (default to 26)
	maType := openapiclient.MaTypeEnum("SMA") // MaTypeEnum | The type of moving average used (optional) (default to "SMA")
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesPpo(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).FastPeriod(fastPeriod).SlowPeriod(slowPeriod).MaType(maType).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesPpo``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesPpo`: GetTimeSeriesPpo200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesPpo`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesPpoRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **fastPeriod** | **int64** | Number of periods for fast moving average. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 12]
 **slowPeriod** | **int64** | Number of periods for slow moving average. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 26]
 **maType** | [**MaTypeEnum**](MaTypeEnum.md) | The type of moving average used | [default to &quot;SMA&quot;]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesPpo200Response**](GetTimeSeriesPpo200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesRoc

> GetTimeSeriesRoc200Response GetTimeSeriesRoc(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Rate of change



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesRoc(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesRoc``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesRoc`: GetTimeSeriesRoc200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesRoc`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesRocRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesRoc200Response**](GetTimeSeriesRoc200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesRocp

> GetTimeSeriesRocp200Response GetTimeSeriesRocp(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Rate of change percentage



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesRocp(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesRocp``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesRocp`: GetTimeSeriesRocp200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesRocp`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesRocpRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesRocp200Response**](GetTimeSeriesRocp200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesRocr

> GetTimeSeriesRocr200Response GetTimeSeriesRocr(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Rate of change ratio



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesRocr(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesRocr``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesRocr`: GetTimeSeriesRocr200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesRocr`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesRocrRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesRocr200Response**](GetTimeSeriesRocr200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesRocr100

> GetTimeSeriesRocr100200Response GetTimeSeriesRocr100(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Rate of change ratio 100



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesRocr100(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesRocr100``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesRocr100`: GetTimeSeriesRocr100200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesRocr100`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesRocr100Request struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesRocr100200Response**](GetTimeSeriesRocr100200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesRsi

> GetTimeSeriesRsi200Response GetTimeSeriesRsi(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Relative strength index



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 14)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesRsi(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesRsi``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesRsi`: GetTimeSeriesRsi200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesRsi`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesRsiRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 14]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesRsi200Response**](GetTimeSeriesRsi200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesRvol

> GetTimeSeriesRvol200Response GetTimeSeriesRvol(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Relative volume



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 14)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesRvol(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesRvol``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesRvol`: GetTimeSeriesRvol200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesRvol`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesRvolRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 14]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesRvol200Response**](GetTimeSeriesRvol200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesSar

> GetTimeSeriesSar200Response GetTimeSeriesSar(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).Acceleration(acceleration).Maximum(maximum).IncludeOhlc(includeOhlc).Execute()

Parabolic stop and reverse



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	acceleration := float64(1.2) // float64 | The rate of change in the indicator's values. (optional) (default to 0.02)
	maximum := float64(1.2) // float64 | The maximum value considered for the indicator calculation. (optional) (default to 0.2)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesSar(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).Acceleration(acceleration).Maximum(maximum).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesSar``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesSar`: GetTimeSeriesSar200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesSar`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesSarRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **acceleration** | **float64** | The rate of change in the indicator&#39;s values. | [default to 0.02]
 **maximum** | **float64** | The maximum value considered for the indicator calculation. | [default to 0.2]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesSar200Response**](GetTimeSeriesSar200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesSarExt

> GetTimeSeriesSarExt200Response GetTimeSeriesSarExt(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).StartValue(startValue).OffsetOnReverse(offsetOnReverse).AccelerationLimitLong(accelerationLimitLong).AccelerationLong(accelerationLong).AccelerationMaxLong(accelerationMaxLong).AccelerationLimitShort(accelerationLimitShort).AccelerationShort(accelerationShort).AccelerationMaxShort(accelerationMaxShort).IncludeOhlc(includeOhlc).Execute()

Parabolic stop and reverse extended



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	startValue := float64(1.2) // float64 | The initial value for the indicator calculation. (optional) (default to 0)
	offsetOnReverse := float64(1.2) // float64 | The adjustment applied when the indicator's direction changes. (optional) (default to 0)
	accelerationLimitLong := float64(1.2) // float64 | The maximum acceleration value for long positions. (optional) (default to 0.02)
	accelerationLong := float64(1.2) // float64 | The acceleration value for long positions. (optional) (default to 0.02)
	accelerationMaxLong := float64(1.2) // float64 | The highest allowed acceleration for long positions. (optional) (default to 0.2)
	accelerationLimitShort := float64(1.2) // float64 | The maximum acceleration value for short positions. (optional) (default to 0.02)
	accelerationShort := float64(1.2) // float64 | The acceleration value for short positions. (optional) (default to 0.02)
	accelerationMaxShort := float64(1.2) // float64 | The highest allowed acceleration for short positions. (optional) (default to 0.2)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesSarExt(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).StartValue(startValue).OffsetOnReverse(offsetOnReverse).AccelerationLimitLong(accelerationLimitLong).AccelerationLong(accelerationLong).AccelerationMaxLong(accelerationMaxLong).AccelerationLimitShort(accelerationLimitShort).AccelerationShort(accelerationShort).AccelerationMaxShort(accelerationMaxShort).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesSarExt``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesSarExt`: GetTimeSeriesSarExt200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesSarExt`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesSarExtRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **startValue** | **float64** | The initial value for the indicator calculation. | [default to 0]
 **offsetOnReverse** | **float64** | The adjustment applied when the indicator&#39;s direction changes. | [default to 0]
 **accelerationLimitLong** | **float64** | The maximum acceleration value for long positions. | [default to 0.02]
 **accelerationLong** | **float64** | The acceleration value for long positions. | [default to 0.02]
 **accelerationMaxLong** | **float64** | The highest allowed acceleration for long positions. | [default to 0.2]
 **accelerationLimitShort** | **float64** | The maximum acceleration value for short positions. | [default to 0.02]
 **accelerationShort** | **float64** | The acceleration value for short positions. | [default to 0.02]
 **accelerationMaxShort** | **float64** | The highest allowed acceleration for short positions. | [default to 0.2]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesSarExt200Response**](GetTimeSeriesSarExt200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesSma

> GetTimeSeriesSma200Response GetTimeSeriesSma(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Simple moving average



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesSma(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesSma``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesSma`: GetTimeSeriesSma200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesSma`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesSmaRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesSma200Response**](GetTimeSeriesSma200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesSqrt

> GetTimeSeriesSqrt200Response GetTimeSeriesSqrt(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).IncludeOhlc(includeOhlc).Execute()

Square root



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesSqrt(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesSqrt``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesSqrt`: GetTimeSeriesSqrt200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesSqrt`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesSqrtRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesSqrt200Response**](GetTimeSeriesSqrt200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesStdDev

> GetTimeSeriesStdDev200Response GetTimeSeriesStdDev(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).Sd(sd).IncludeOhlc(includeOhlc).Execute()

Standard deviation



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	sd := float64(1.2) // float64 | The standard deviation applied in the calculation. (optional) (default to 2)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesStdDev(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).Sd(sd).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesStdDev``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesStdDev`: GetTimeSeriesStdDev200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesStdDev`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesStdDevRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **sd** | **float64** | The standard deviation applied in the calculation. | [default to 2]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesStdDev200Response**](GetTimeSeriesStdDev200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesStoch

> GetTimeSeriesStoch200Response GetTimeSeriesStoch(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).FastKPeriod(fastKPeriod).SlowKPeriod(slowKPeriod).SlowDPeriod(slowDPeriod).SlowKmaType(slowKmaType).SlowDmaType(slowDmaType).IncludeOhlc(includeOhlc).Execute()

Stochastic oscillator



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	fastKPeriod := int64(789) // int64 | The time period for the fast %K line in the Stochastic Oscillator. Takes values in the range from `1` to `800` (optional) (default to 14)
	slowKPeriod := int64(789) // int64 | The time period for the slow %K line in the Stochastic Oscillator. Takes values in the range from `1` to `800` (optional) (default to 1)
	slowDPeriod := int64(789) // int64 | The time period for the slow %D line in the Stochastic Oscillator. Takes values in the range from `1` to `800` (optional) (default to 3)
	slowKmaType := openapiclient.MaTypeEnum("SMA") // MaTypeEnum | The type of slow %K Moving Average used. Default is SMA. (optional) (default to "SMA")
	slowDmaType := openapiclient.MaTypeEnum("SMA") // MaTypeEnum | The type of slow Displaced Moving Average used. Default is SMA. (optional) (default to "SMA")
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesStoch(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).FastKPeriod(fastKPeriod).SlowKPeriod(slowKPeriod).SlowDPeriod(slowDPeriod).SlowKmaType(slowKmaType).SlowDmaType(slowDmaType).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesStoch``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesStoch`: GetTimeSeriesStoch200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesStoch`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesStochRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **fastKPeriod** | **int64** | The time period for the fast %K line in the Stochastic Oscillator. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 14]
 **slowKPeriod** | **int64** | The time period for the slow %K line in the Stochastic Oscillator. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 1]
 **slowDPeriod** | **int64** | The time period for the slow %D line in the Stochastic Oscillator. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 3]
 **slowKmaType** | [**MaTypeEnum**](MaTypeEnum.md) | The type of slow %K Moving Average used. Default is SMA. | [default to &quot;SMA&quot;]
 **slowDmaType** | [**MaTypeEnum**](MaTypeEnum.md) | The type of slow Displaced Moving Average used. Default is SMA. | [default to &quot;SMA&quot;]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesStoch200Response**](GetTimeSeriesStoch200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesStochF

> GetTimeSeriesStochF200Response GetTimeSeriesStochF(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).FastKPeriod(fastKPeriod).FastDPeriod(fastDPeriod).FastDmaType(fastDmaType).IncludeOhlc(includeOhlc).Execute()

Stochastic fast



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	fastKPeriod := int64(789) // int64 | The time period for the fast %K line in the Stochastic Oscillator. Takes values in the range from `1` to `800` (optional) (default to 14)
	fastDPeriod := int64(789) // int64 | The time period for the fast %D line in the Stochastic Oscillator. Takes values in the range from `1` to `800` (optional) (default to 3)
	fastDmaType := openapiclient.MaTypeEnum("SMA") // MaTypeEnum | The type of fast Displaced Moving Average used. (optional) (default to "SMA")
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesStochF(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).FastKPeriod(fastKPeriod).FastDPeriod(fastDPeriod).FastDmaType(fastDmaType).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesStochF``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesStochF`: GetTimeSeriesStochF200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesStochF`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesStochFRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **fastKPeriod** | **int64** | The time period for the fast %K line in the Stochastic Oscillator. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 14]
 **fastDPeriod** | **int64** | The time period for the fast %D line in the Stochastic Oscillator. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 3]
 **fastDmaType** | [**MaTypeEnum**](MaTypeEnum.md) | The type of fast Displaced Moving Average used. | [default to &quot;SMA&quot;]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesStochF200Response**](GetTimeSeriesStochF200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesStochRsi

> GetTimeSeriesStochRsi200Response GetTimeSeriesStochRsi(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).RsiLength(rsiLength).StochLength(stochLength).KPeriod(kPeriod).SlowKmaType(slowKmaType).DPeriod(dPeriod).SlowDmaType(slowDmaType).IncludeOhlc(includeOhlc).Execute()

Stochastic relative strength index



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeStochrsiEnum("open") // SeriesTypeStochrsiEnum | Specifies the price data type: open, high, low, or close. (optional) (default to "close")
	rsiLength := int64(789) // int64 | Length of period for calculating the RSI component. Takes values in the range from `1` to `800` (optional) (default to 14)
	stochLength := int64(789) // int64 | Period length for computing the stochastic oscillator of the RSI. Takes values in the range from `1` to `800` (optional) (default to 14)
	kPeriod := int64(789) // int64 | Period for smoothing the %K line. Takes values in the range from `1` to `800` (optional) (default to 3)
	slowKmaType := "slowKmaType_example" // string |  (optional)
	dPeriod := int64(789) // int64 | Period for smoothing the %D line, which is a moving average of %K. Takes values in the range from `1` to `800` (optional) (default to 3)
	slowDmaType := "slowDmaType_example" // string |  (optional)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesStochRsi(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).RsiLength(rsiLength).StochLength(stochLength).KPeriod(kPeriod).SlowKmaType(slowKmaType).DPeriod(dPeriod).SlowDmaType(slowDmaType).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesStochRsi``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesStochRsi`: GetTimeSeriesStochRsi200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesStochRsi`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesStochRsiRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeStochrsiEnum**](SeriesTypeStochrsiEnum.md) | Specifies the price data type: open, high, low, or close. | [default to &quot;close&quot;]
 **rsiLength** | **int64** | Length of period for calculating the RSI component. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 14]
 **stochLength** | **int64** | Period length for computing the stochastic oscillator of the RSI. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 14]
 **kPeriod** | **int64** | Period for smoothing the %K line. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 3]
 **slowKmaType** | **string** |  | 
 **dPeriod** | **int64** | Period for smoothing the %D line, which is a moving average of %K. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 3]
 **slowDmaType** | **string** |  | 
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesStochRsi200Response**](GetTimeSeriesStochRsi200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesSub

> GetTimeSeriesSub200Response GetTimeSeriesSub(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType1(seriesType1).SeriesType2(seriesType2).IncludeOhlc(includeOhlc).Execute()

Subtraction



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType1 := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type used as the first part of technical indicator (optional) (default to "open")
	seriesType2 := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type used as the second part of technical indicator (optional) (default to "open")
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesSub(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType1(seriesType1).SeriesType2(seriesType2).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesSub``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesSub`: GetTimeSeriesSub200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesSub`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesSubRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType1** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type used as the first part of technical indicator | [default to &quot;open&quot;]
 **seriesType2** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type used as the second part of technical indicator | [default to &quot;open&quot;]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesSub200Response**](GetTimeSeriesSub200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesSum

> GetTimeSeriesSum200Response GetTimeSeriesSum(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Summation



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesSum(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesSum``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesSum`: GetTimeSeriesSum200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesSum`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesSumRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesSum200Response**](GetTimeSeriesSum200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesSuperTrend

> GetTimeSeriesSuperTrend200Response GetTimeSeriesSuperTrend(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).Period(period).Multiplier(multiplier).IncludeOhlc(includeOhlc).Execute()

Supertrend



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	period := int64(789) // int64 | The period used for calculation in the indicator. Takes values in the range from `1` to `800` (optional) (default to 10)
	multiplier := int64(789) // int64 | The factor used to adjust the indicator's sensitivity. (optional) (default to 3)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesSuperTrend(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).Period(period).Multiplier(multiplier).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesSuperTrend``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesSuperTrend`: GetTimeSeriesSuperTrend200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesSuperTrend`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesSuperTrendRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **period** | **int64** | The period used for calculation in the indicator. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 10]
 **multiplier** | **int64** | The factor used to adjust the indicator&#39;s sensitivity. | [default to 3]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesSuperTrend200Response**](GetTimeSeriesSuperTrend200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesSuperTrendHeikinAshiCandles

> GetTimeSeriesSuperTrendHeikinAshiCandles200Response GetTimeSeriesSuperTrendHeikinAshiCandles(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).Period(period).Multiplier(multiplier).IncludeOhlc(includeOhlc).Execute()

Supertrend Heikin Ashi candles



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	period := int64(789) // int64 | The period used for calculation in the indicator. Takes values in the range from `1` to `800` (optional) (default to 10)
	multiplier := int64(789) // int64 | The factor used to adjust the indicator's sensitivity. (optional) (default to 3)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesSuperTrendHeikinAshiCandles(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).Period(period).Multiplier(multiplier).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesSuperTrendHeikinAshiCandles``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesSuperTrendHeikinAshiCandles`: GetTimeSeriesSuperTrendHeikinAshiCandles200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesSuperTrendHeikinAshiCandles`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesSuperTrendHeikinAshiCandlesRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **period** | **int64** | The period used for calculation in the indicator. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 10]
 **multiplier** | **int64** | The factor used to adjust the indicator&#39;s sensitivity. | [default to 3]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesSuperTrendHeikinAshiCandles200Response**](GetTimeSeriesSuperTrendHeikinAshiCandles200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesT3ma

> GetTimeSeriesT3ma200Response GetTimeSeriesT3ma(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).VFactor(vFactor).IncludeOhlc(includeOhlc).Execute()

Triple exponential moving average



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	vFactor := float64(1.2) // float64 | The factor used to adjust the indicator's volatility. Takes values in the range from `0` to `1` (optional) (default to 0.7)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesT3ma(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).VFactor(vFactor).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesT3ma``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesT3ma`: GetTimeSeriesT3ma200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesT3ma`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesT3maRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **vFactor** | **float64** | The factor used to adjust the indicator&#39;s volatility. Takes values in the range from &#x60;0&#x60; to &#x60;1&#x60; | [default to 0.7]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesT3ma200Response**](GetTimeSeriesT3ma200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesTRange

> GetTimeSeriesTRange200Response GetTimeSeriesTRange(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).IncludeOhlc(includeOhlc).Execute()

True range



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesTRange(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesTRange``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesTRange`: GetTimeSeriesTRange200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesTRange`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesTRangeRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesTRange200Response**](GetTimeSeriesTRange200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesTema

> GetTimeSeriesTema200Response GetTimeSeriesTema(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Triple exponential moving average



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	timePeriod := int64(789) // int64 | The time period used for calculation in the indicator. Default is 9. (optional) (default to 9)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesTema(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesTema``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesTema`: GetTimeSeriesTema200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesTema`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesTemaRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **timePeriod** | **int64** | The time period used for calculation in the indicator. Default is 9. | [default to 9]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesTema200Response**](GetTimeSeriesTema200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesTrima

> GetTimeSeriesTrima200Response GetTimeSeriesTrima(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Triangular moving average



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesTrima(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesTrima``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesTrima`: GetTimeSeriesTrima200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesTrima`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesTrimaRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesTrima200Response**](GetTimeSeriesTrima200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesTsf

> GetTimeSeriesTsf200Response GetTimeSeriesTsf(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Time series forecast



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesTsf(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesTsf``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesTsf`: GetTimeSeriesTsf200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesTsf`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesTsfRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesTsf200Response**](GetTimeSeriesTsf200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesTypPrice

> GetTimeSeriesTypPrice200Response GetTimeSeriesTypPrice(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).IncludeOhlc(includeOhlc).Execute()

Typical price



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesTypPrice(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesTypPrice``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesTypPrice`: GetTimeSeriesTypPrice200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesTypPrice`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesTypPriceRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesTypPrice200Response**](GetTimeSeriesTypPrice200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesUltOsc

> GetTimeSeriesUltOsc200Response GetTimeSeriesUltOsc(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod1(timePeriod1).TimePeriod2(timePeriod2).TimePeriod3(timePeriod3).IncludeOhlc(includeOhlc).Execute()

Ultimate oscillator endpoint



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	timePeriod1 := int64(789) // int64 | The first time period used for calculation in the indicator. Takes values in the range from `1` to `800` (optional) (default to 7)
	timePeriod2 := int64(789) // int64 | The second time period used for calculation in the indicator. Takes values in the range from `1` to `800` (optional) (default to 14)
	timePeriod3 := int64(789) // int64 | The third time period used for calculation in the indicator. Takes values in the range from `1` to `800` (optional) (default to 28)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesUltOsc(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod1(timePeriod1).TimePeriod2(timePeriod2).TimePeriod3(timePeriod3).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesUltOsc``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesUltOsc`: GetTimeSeriesUltOsc200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesUltOsc`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesUltOscRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **timePeriod1** | **int64** | The first time period used for calculation in the indicator. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 7]
 **timePeriod2** | **int64** | The second time period used for calculation in the indicator. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 14]
 **timePeriod3** | **int64** | The third time period used for calculation in the indicator. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 28]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesUltOsc200Response**](GetTimeSeriesUltOsc200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesVar

> GetTimeSeriesVar200Response GetTimeSeriesVar(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Variance



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesVar(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesVar``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesVar`: GetTimeSeriesVar200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesVar`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesVarRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesVar200Response**](GetTimeSeriesVar200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesVwap

> GetTimeSeriesVwap200Response GetTimeSeriesVwap(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SdTimePeriod(sdTimePeriod).Sd(sd).IncludeOhlc(includeOhlc).Execute()

Volume weighted average price



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	sdTimePeriod := int64(789) // int64 | The time period for the standard deviation calculation. Must be greater than `0`. Recommended value is `9`. This parameter is only used together with `sd`. (optional) (default to 0)
	sd := float64(1.2) // float64 | The standard deviation applied in the calculation. Must be greater than `0`. Recommended value is `2`. This parameter is only used together with `sd_time_period`. (optional) (default to 0)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesVwap(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SdTimePeriod(sdTimePeriod).Sd(sd).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesVwap``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesVwap`: GetTimeSeriesVwap200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesVwap`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesVwapRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **sdTimePeriod** | **int64** | The time period for the standard deviation calculation. Must be greater than &#x60;0&#x60;. Recommended value is &#x60;9&#x60;. This parameter is only used together with &#x60;sd&#x60;. | [default to 0]
 **sd** | **float64** | The standard deviation applied in the calculation. Must be greater than &#x60;0&#x60;. Recommended value is &#x60;2&#x60;. This parameter is only used together with &#x60;sd_time_period&#x60;. | [default to 0]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesVwap200Response**](GetTimeSeriesVwap200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesWclPrice

> GetTimeSeriesWclPrice200Response GetTimeSeriesWclPrice(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).IncludeOhlc(includeOhlc).Execute()

Weighted close price



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesWclPrice(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesWclPrice``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesWclPrice`: GetTimeSeriesWclPrice200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesWclPrice`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesWclPriceRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesWclPrice200Response**](GetTimeSeriesWclPrice200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesWillR

> GetTimeSeriesWillR200Response GetTimeSeriesWillR(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Williams %R



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 14)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesWillR(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesWillR``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesWillR`: GetTimeSeriesWillR200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesWillR`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesWillRRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 14]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesWillR200Response**](GetTimeSeriesWillR200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)


## GetTimeSeriesWma

> GetTimeSeriesWma200Response GetTimeSeriesWma(ctx).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()

Weighted moving average



### Example

```go
package main

import (
	"context"
	"fmt"
	"os"
	openapiclient "github.com/twelvedata/twelvedata-go/twelvedata"
)

func main() {
	interval := openapiclient.IntervalEnum("1min") // IntervalEnum | Interval between two consecutive points in time series
	symbol := "AAPL" // string | Symbol ticker of the instrument. E.g. `AAPL`, `EUR/USD`, `ETH/BTC`, ... (optional)
	isin := "US0378331005" // string | Filter by international securities identification number (ISIN). ISIN access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	figi := "BBG000B9Y5X2" // string | The FIGI of an instrument for which data is requested. This parameter is available on the <a href=\"https://twelvedata.com/pricing\">Ultra</a> plan (individual) and the <a href=\"https://twelvedata.com/pricing-business\">Enterprise</a> plan (business) and above. (optional)
	cusip := "594918104" // string | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the <a href=\"https://twelvedata.com/account/add-ons\">Data add-ons</a> section (optional)
	outputsize := int64(789) // int64 | Number of data points to retrieve. Supports values in the range from `1` to `5000`. Default `30` when no date parameters are set, otherwise set to maximum (optional) (default to 30)
	exchange := "NASDAQ" // string | Exchange where instrument is traded (optional)
	micCode := "XNAS" // string | Market Identifier Code (MIC) under ISO 10383 standard (optional)
	country := "United States" // string | The country where the instrument is traded, e.g., `United States` or `US` (optional)
	type_ := openapiclient.TypeEnum("American Depositary Receipt") // TypeEnum | The asset class to which the instrument belongs (optional)
	timezone := "timezone_example" // string | Timezone at which output datetime will be displayed. Supports: <ul> <li>1. <code>Exchange</code> for local exchange time</li> <li>2. <code>UTC</code> for datetime at universal UTC standard</li> <li>3. Timezone name according to the IANA Time Zone Database. E.g. <code>America/New_York</code>, <code>Asia/Singapore</code>. Full list of timezones can be found <a href=\"https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\" target=\"blank\">here</a></li> </ul> <p>Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.</p> <i>Take note that the IANA Timezone name is case-sensitive</i> (optional) (default to "Exchange")
	startDate := "2024-08-22T15:04:05" // string | Can be used separately and together with `end_date`. Format `2006-01-02` or `2006-01-02T15:04:05`  Default location: <ul> <li>Forex and Cryptocurrencies - <code>UTC</code></li> <li>Stocks - where exchange is located (e.g. for AAPL it will be <code>America/New_York</code>)</li> </ul> Both parameters take into account if <code>timezone</code> parameter is provided.<br/> If <code>timezone</code> is given then, <code>start_date</code> and <code>end_date</code> will be used in the specified location  Examples: <ul> <li>1. <code>&symbol=AAPL&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 New York time up to current date</li> <li>2. <code>&symbol=EUR/USD&timezone=Asia/Singapore&start_date=2019-08-09T15:50:00&…</code><br/> Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date</li> <li>3. <code>&symbol=ETH/BTC&timezone=Europe/Zurich&start_date=2019-08-09T15:50:00&end_date=2019-08-09T15:55:00&...</code><br/> Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00</li> </ul> (optional)
	endDate := "2024-08-22T16:04:05" // string | The ending date and time for data selection, see `start_date` description for details. (optional)
	date := "2021-10-27" // string | Specifies the exact date to get the data for. Could be the exact date, e.g. `2021-10-27`, or in human language `today` or `yesterday` (optional)
	order := openapiclient.OrderEnum("asc") // OrderEnum | Sorting order of the output (optional) (default to "desc")
	prepost := true // bool | Returns quotes that include pre-market and post-market data. Only for the `Pro` plan (individual) and `Venture` plan (business) and above. Available at the `1min`, `5min`, `15min`, and `30min` intervals for US equities. Open, high, low, close values are supplied without volume (optional) (default to false)
	format := openapiclient.FormatEnum("JSON") // FormatEnum | The format of the response data (optional) (default to "JSON")
	delimiter := "delimiter_example" // string | The separator used in the CSV response data (optional) (default to ";")
	dp := int64(789) // int64 | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided (optional) (default to -1)
	previousClose := true // bool | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object (optional) (default to false)
	adjust := openapiclient.AdjustEnum("all") // AdjustEnum | Adjusting mode for prices (optional) (default to "splits")
	seriesType := openapiclient.SeriesTypeEnum("close") // SeriesTypeEnum | Price type on which technical indicator is calculated (optional) (default to "open")
	timePeriod := int64(789) // int64 | Number of periods to average over. Takes values in the range from `1` to `800` (optional) (default to 9)
	includeOhlc := true // bool | Specify if OHLC values should be added in the output (optional) (default to false)

	configuration := openapiclient.NewConfiguration()
	apiClient := openapiclient.NewAPIClient(configuration)
	resp, r, err := apiClient.TechnicalIndicatorAPI.GetTimeSeriesWma(context.Background()).Interval(interval).Symbol(symbol).Isin(isin).Figi(figi).Cusip(cusip).Outputsize(outputsize).Exchange(exchange).MicCode(micCode).Country(country).Type_(type_).Timezone(timezone).StartDate(startDate).EndDate(endDate).Date(date).Order(order).Prepost(prepost).Format(format).Delimiter(delimiter).Dp(dp).PreviousClose(previousClose).Adjust(adjust).SeriesType(seriesType).TimePeriod(timePeriod).IncludeOhlc(includeOhlc).Execute()
	if err != nil {
		fmt.Fprintf(os.Stderr, "Error when calling `TechnicalIndicatorAPI.GetTimeSeriesWma``: %v\n", err)
		fmt.Fprintf(os.Stderr, "Full HTTP response: %v\n", r)
	}
	// response from `GetTimeSeriesWma`: GetTimeSeriesWma200Response
	fmt.Fprintf(os.Stdout, "Response from `TechnicalIndicatorAPI.GetTimeSeriesWma`: %v\n", resp)
}
```

### Path Parameters



### Other Parameters

Other parameters are passed through a pointer to a apiGetTimeSeriesWmaRequest struct via the builder pattern


Name | Type | Description  | Notes
------------- | ------------- | ------------- | -------------
 **interval** | [**IntervalEnum**](IntervalEnum.md) | Interval between two consecutive points in time series | 
 **symbol** | **string** | Symbol ticker of the instrument. E.g. &#x60;AAPL&#x60;, &#x60;EUR/USD&#x60;, &#x60;ETH/BTC&#x60;, ... | 
 **isin** | **string** | Filter by international securities identification number (ISIN). ISIN access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **figi** | **string** | The FIGI of an instrument for which data is requested. This parameter is available on the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing\&quot;&gt;Ultra&lt;/a&gt; plan (individual) and the &lt;a href&#x3D;\&quot;https://twelvedata.com/pricing-business\&quot;&gt;Enterprise&lt;/a&gt; plan (business) and above. | 
 **cusip** | **string** | The CUSIP of an instrument for which data is requested. CUSIP access is activating in the &lt;a href&#x3D;\&quot;https://twelvedata.com/account/add-ons\&quot;&gt;Data add-ons&lt;/a&gt; section | 
 **outputsize** | **int64** | Number of data points to retrieve. Supports values in the range from &#x60;1&#x60; to &#x60;5000&#x60;. Default &#x60;30&#x60; when no date parameters are set, otherwise set to maximum | [default to 30]
 **exchange** | **string** | Exchange where instrument is traded | 
 **micCode** | **string** | Market Identifier Code (MIC) under ISO 10383 standard | 
 **country** | **string** | The country where the instrument is traded, e.g., &#x60;United States&#x60; or &#x60;US&#x60; | 
 **type_** | [**TypeEnum**](TypeEnum.md) | The asset class to which the instrument belongs | 
 **timezone** | **string** | Timezone at which output datetime will be displayed. Supports: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;Exchange&lt;/code&gt; for local exchange time&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;UTC&lt;/code&gt; for datetime at universal UTC standard&lt;/li&gt; &lt;li&gt;3. Timezone name according to the IANA Time Zone Database. E.g. &lt;code&gt;America/New_York&lt;/code&gt;, &lt;code&gt;Asia/Singapore&lt;/code&gt;. Full list of timezones can be found &lt;a href&#x3D;\&quot;https://en.wikipedia.org/wiki/List_of_tz_database_time_zones\&quot; target&#x3D;\&quot;blank\&quot;&gt;here&lt;/a&gt;&lt;/li&gt; &lt;/ul&gt; &lt;p&gt;Interval Limitation: The timezone parameter is only applicable for intraday intervals (less than 1 day). For intervals of 1day, 1week, or 1month, the timezone parameter is ignored, and data is strictly returned in the Exchange local time.&lt;/p&gt; &lt;i&gt;Take note that the IANA Timezone name is case-sensitive&lt;/i&gt; | [default to &quot;Exchange&quot;]
 **startDate** | **string** | Can be used separately and together with &#x60;end_date&#x60;. Format &#x60;2006-01-02&#x60; or &#x60;2006-01-02T15:04:05&#x60;  Default location: &lt;ul&gt; &lt;li&gt;Forex and Cryptocurrencies - &lt;code&gt;UTC&lt;/code&gt;&lt;/li&gt; &lt;li&gt;Stocks - where exchange is located (e.g. for AAPL it will be &lt;code&gt;America/New_York&lt;/code&gt;)&lt;/li&gt; &lt;/ul&gt; Both parameters take into account if &lt;code&gt;timezone&lt;/code&gt; parameter is provided.&lt;br/&gt; If &lt;code&gt;timezone&lt;/code&gt; is given then, &lt;code&gt;start_date&lt;/code&gt; and &lt;code&gt;end_date&lt;/code&gt; will be used in the specified location  Examples: &lt;ul&gt; &lt;li&gt;1. &lt;code&gt;&amp;symbol&#x3D;AAPL&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 New York time up to current date&lt;/li&gt; &lt;li&gt;2. &lt;code&gt;&amp;symbol&#x3D;EUR/USD&amp;timezone&#x3D;Asia/Singapore&amp;start_date&#x3D;2019-08-09T15:50:00&amp;…&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Singapore time up to current date&lt;/li&gt; &lt;li&gt;3. &lt;code&gt;&amp;symbol&#x3D;ETH/BTC&amp;timezone&#x3D;Europe/Zurich&amp;start_date&#x3D;2019-08-09T15:50:00&amp;end_date&#x3D;2019-08-09T15:55:00&amp;...&lt;/code&gt;&lt;br/&gt; Returns all records starting from 2019-08-09T15:50:00 Zurich time up to 2019-08-09T15:55:00&lt;/li&gt; &lt;/ul&gt; | 
 **endDate** | **string** | The ending date and time for data selection, see &#x60;start_date&#x60; description for details. | 
 **date** | **string** | Specifies the exact date to get the data for. Could be the exact date, e.g. &#x60;2021-10-27&#x60;, or in human language &#x60;today&#x60; or &#x60;yesterday&#x60; | 
 **order** | [**OrderEnum**](OrderEnum.md) | Sorting order of the output | [default to &quot;desc&quot;]
 **prepost** | **bool** | Returns quotes that include pre-market and post-market data. Only for the &#x60;Pro&#x60; plan (individual) and &#x60;Venture&#x60; plan (business) and above. Available at the &#x60;1min&#x60;, &#x60;5min&#x60;, &#x60;15min&#x60;, and &#x60;30min&#x60; intervals for US equities. Open, high, low, close values are supplied without volume | [default to false]
 **format** | [**FormatEnum**](FormatEnum.md) | The format of the response data | [default to &quot;JSON&quot;]
 **delimiter** | **string** | The separator used in the CSV response data | [default to &quot;;&quot;]
 **dp** | **int64** | Specifies the number of decimal places for floating values. Should be in range [0, 11] inclusive. By default, the number of decimal places is automatically determined based on the values provided | [default to -1]
 **previousClose** | **bool** | A boolean parameter to include the previous closing price in the time_series data. If true, adds previous bar close price value to the current object | [default to false]
 **adjust** | [**AdjustEnum**](AdjustEnum.md) | Adjusting mode for prices | [default to &quot;splits&quot;]
 **seriesType** | [**SeriesTypeEnum**](SeriesTypeEnum.md) | Price type on which technical indicator is calculated | [default to &quot;open&quot;]
 **timePeriod** | **int64** | Number of periods to average over. Takes values in the range from &#x60;1&#x60; to &#x60;800&#x60; | [default to 9]
 **includeOhlc** | **bool** | Specify if OHLC values should be added in the output | [default to false]

### Return type

[**GetTimeSeriesWma200Response**](GetTimeSeriesWma200Response.md)

### Authorization

[authorizationHeader](../README.md#authorizationHeader)

### HTTP request headers

- **Content-Type**: Not defined
- **Accept**: application/json

[[Back to top]](#) [[Back to API list]](../README.md#documentation-for-api-endpoints)
[[Back to Model list]](../README.md#documentation-for-models)
[[Back to README]](../README.md)

